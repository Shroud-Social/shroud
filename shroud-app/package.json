{
  "name": "shroud-app",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "mobile": "pnpm --filter shroud-mobile dev",
    "desktop": "pnpm --filter shroud-desktop dev",
    "web": "pnpm --filter shroud-web dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  }
}